# Database Configuration
DATABASE_URL=postgres://postgres:hesoyam@127.0.0.1:5432/gorica_calendar

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration
# In development mode, all localhost origins are allowed automatically
# For production, specify allowed origins (comma-separated for multiple)
FRONTEND_URL=http://localhost:5173,http://localhost:5174

# JWT Configuration
# IMPORTANT: Change this to a strong, random secret in production!
# Generate a secure secret using: node scripts/generate_jwt_secret.js
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# Password Security
# Set to 'true' to enforce strict password requirements (8+ chars, uppercase, lowercase, number, special char)
# In production mode, strict validation is automatically enabled
STRICT_PASSWORD_VALIDATION=false

